meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11

build:
  args: --no-cache-dir

run:
  command: python echobot.py
  persistenceMount: /data

env:
  PORT: 8080
  BOT_TOKEN: ${BOT_TOKEN} 